// Shared Tailwind class strings for the report tables — same visual language
// as the ping-test result table (GlobalLatencyTest.vue). Kept as constants so
// the eleven renderers stay literally consistent.

export const TH_LEFT = 'px-3 py-2.5 text-xs font-medium text-muted-foreground uppercase tracking-wide text-left text-nowrap';
export const TH_RIGHT = 'px-3 py-2.5 text-xs font-medium text-muted-foreground uppercase tracking-wide text-right text-nowrap';
export const TD_NUM = 'px-3 py-2 text-right font-mono tabular-nums';
